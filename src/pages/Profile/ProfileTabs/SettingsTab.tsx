
import Settings from '../Settings';

const SettingsTab = () => {
  return (
    <div>
      {/* Add Account Settings related features here */}
      <Settings />
    </div>
  );
};

export default SettingsTab;
